name: Deploy Chimpions Dash

on:
  push:
    branches:
      - main
    paths:
      - 'dist/**'
      - 'engine-qa.mjs'
      - 'qa.cjs'
      - 'asset-qa.mjs'
      - 'prepare-deploy.mjs'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  test:
    name: Validate game
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Run deterministic engine QA
        run: node engine-qa.mjs

      - name: Run UI QA
        run: node qa.cjs

      - name: Run asset integrity QA
        run: node asset-qa.mjs

      - name: Test production packaging
        env:
          GITHUB_SHA: test-build-000000000000000000000000000000000000
        run: |
          node prepare-deploy.mjs
          node --check site/app.js
          node --check site/engine.js
          node --check site/render.js
          node --check site/audio.js

  deploy:
    name: Deploy GitHub Pages
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Prepare cache-busted production build
        run: node prepare-deploy.mjs

      - name: Upload game
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
